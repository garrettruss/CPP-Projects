// Replace these declarations with a template

template <typename T>
T get_smallest(T num1, T num2)
{

    return num2 < num1 ? num2 : num1;
    // C++ functions don’t require a return value, but they do require a return type.
}
